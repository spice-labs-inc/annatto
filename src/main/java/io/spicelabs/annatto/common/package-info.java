/**
 * Common infrastructure shared across all ecosystem implementations.
 * Includes normalized metadata types (tested by {@code ParsedDependencyTest}),
 * PURL construction (tested by {@code PurlBuilderTest}),
 * ecosystem identifiers (tested by {@code EcosystemIdTest}),
 * and metadata mapping (tested by {@code MetadataMapperTest}).
 */
package io.spicelabs.annatto.common;
