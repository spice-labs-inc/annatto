/**
 * Process filter that dispatches artifacts to ecosystem-specific handlers
 * based on filename extension and MIME type.
 * (tested by {@code AnnattoProcessFilterTest.detectEcosystem_*} — 20+ filter routing tests)
 */
package io.spicelabs.annatto.filter;
