/**
 * Base artifact handler and memento classes providing shared logic
 * for all ecosystem implementations. The {@code doBegin}/{@code doBuildPurls}
 * template pattern is exercised by all 11 per-ecosystem {@code *HandlerTest} classes.
 */
package io.spicelabs.annatto.handler;
